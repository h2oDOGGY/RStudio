library(dplyr) #A fast, consistent tool for working with data frame like objects,
#both in memory and out of memory
library(readr) #Read flat/tabular text files
library(quantmod) #used for stock charting
library(stockPortfolio) # Base package for retrieving returns
library(ggplot2) # Used to graph efficient frontier
library(reshape2) # Used to melt the data
library(quadprog) #Needed for solve.QP
library(tidyr) #Easily Tidy Data with `spread()` 
#and `gather()` Functions